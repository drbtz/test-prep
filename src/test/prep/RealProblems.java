/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package test.prep;

/**
 *
 * @author Dr Beatz
 */
public class RealProblems {

    public static void fizzBuzz() 
    {
        for (int i = 0; i < 100; i++) {
            if (i % 3 == 0 && i % 5 == 0) {
                System.out.println("FizzBuzz");
            } else if (i % 3 == 0) {
                System.out.println("Fizz");
            } else if (i % 5 == 0) {
                System.out.println("Buzz");
            } else {
                System.out.println(i);
            }
        }
    }

    //Hardin Interview Question
    //You are given the head node of a tree, it can have any number of children stored as an array.
    //Each node also has a color (RED, GREEN, ORANGE)
    //Traverse the tree and print out the number of each color on a given level
    
    //Node Class
    //string "color"
    // private Node[] children
    
    public static void numColor() 
    {
        
        
    }
    
    
    /*
Okay, imagine that I've got a sorted list stored in a ring buffer, like so:

[12, 23, 30, 31, 50, 2, 6, 10]

Write a function that returns the index of the start of the sorted list*/
    
    
    
    
    
/*   MITS interview
 * 
 * Write a function/method/subroutine in a language of your choice to find the item that has the highest total quantity.   Input is two parallel arrays with items and quantity received.   Output is the name of the item with the highest total quantity.
Examples of input and output:
Input:
  items =  [“pear”, “apple”, “orange”, “apple”]
  qtys   =  [3,          2,          5,            4]
Ouput:
  “apple”
  * */
    
    
    
}
